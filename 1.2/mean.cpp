#include <iostream>

using namespace std;

int main() {
    int a, b;

    cout << "Inserire due numeri interi: ";
    cin >> a >> b;

    float media = (a + b) / 2.0;
    cout << "Media = " << media << endl;

    return 0;
}